# Task P10.5: Register Phase 2 widgets in chart registry

## Objective

Add 4 new Phase 2 widget entries to the chart registry and verify the full set of 9 widgets (5 Phase 1 + 4 Phase 2) compiles correctly. Also assess whether any existing Phase 1 widgets should be retired — recommendation is to keep all 5 alongside the new ones, as they serve complementary purposes.

## Inputs

- `client/src/components/charts/chartRegistry.ts` — the existing registry with 5 Phase 1 widgets
- `client/src/components/charts/NPVDistributionWidget.tsx` — created in P10.1
- `client/src/components/charts/FiscalWaterfallDistWidget.tsx` — created in P10.2
- `client/src/components/charts/HouseholdNPVWidget.tsx` — created in P10.3
- `client/src/components/charts/PolicyScenarioWidget.tsx` — created in P10.4

## Expected outputs

- `client/src/components/charts/chartRegistry.ts` — updated with 4 new entries (9 total)

## Execution instructions

### Step 1: Read the existing registry

Read `client/src/components/charts/chartRegistry.ts`. Note:
- The structure of existing entries (demo ID, lazy import pattern, display name)
- How the lazy import works: `React.lazy(() => import('./WidgetName').then((m) => ({ default: m.WidgetName })))`
- The TypeScript type for the registry entries
- The order of entries

### Step 2: Assess existing Phase 1 widgets

Review the 5 existing Phase 1 widgets:
1. `npv-calculator` — NPV lifecycle trajectory by visa type and arrival age
2. `fiscal-waterfall` — point-estimate waterfall of fiscal components
3. `retention-explorer` — retention curve visualization
4. `rv2021-shift` — residence visa 2021 cohort analysis
5. `nationality-convergence` — nationality-based fiscal convergence over time

Assessment (to confirm, not change):
- **npv-calculator**: KEEP — shows lifecycle trajectory (time series); complements P10.1's distribution (cross-section). Different views of the same underlying NPV data.
- **fiscal-waterfall**: KEEP — simple point-estimate waterfall is easier to read; the distributional version (P10.2) adds uncertainty bands but is more complex. Both serve different audiences.
- **retention-explorer**: KEEP — standalone retention visualization not replicated in Phase 2.
- **rv2021-shift**: KEEP — specific cohort analysis not replicated in Phase 2.
- **nationality-convergence**: KEEP — nationality convergence over time not replicated in Phase 2.

Conclusion: Keep all 5. Total will be 9 widgets.

### Step 3: Add 4 new entries

Add these entries to the registry, maintaining alphabetical order by demo ID or appending after existing entries (match the existing convention):

```typescript
'npv-distribution': {
  component: React.lazy(() =>
    import('./NPVDistributionWidget').then((m) => ({ default: m.NPVDistributionWidget }))
  ),
  title: 'NPV distribution',
},
'fiscal-waterfall-dist': {
  component: React.lazy(() =>
    import('./FiscalWaterfallDistWidget').then((m) => ({ default: m.FiscalWaterfallDistWidget }))
  ),
  title: 'Fiscal waterfall with distributions',
},
'household-npv': {
  component: React.lazy(() =>
    import('./HouseholdNPVWidget').then((m) => ({ default: m.HouseholdNPVWidget }))
  ),
  title: 'Household NPV',
},
'policy-scenario': {
  component: React.lazy(() =>
    import('./PolicyScenarioWidget').then((m) => ({ default: m.PolicyScenarioWidget }))
  ),
  title: 'Policy scenario explorer',
},
```

Adapt the exact syntax to match the existing registry pattern. If the registry uses a different structure (e.g., array instead of object, different key names), match it exactly.

### Step 4: Verify TypeScript compilation

Run:
```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report && npx tsc --noEmit
```

Fix any TypeScript errors. Common issues:
- Import path typos
- Named export mismatch (component name in `.then((m) => ...)` must match the actual named export)
- Registry type not accepting new keys (may need to update the type definition)

### Step 5: Verify all 9 widgets are registered

Read the updated registry and count entries. Confirm:
- 5 Phase 1: npv-calculator, fiscal-waterfall, retention-explorer, rv2021-shift, nationality-convergence
- 4 Phase 2: npv-distribution, fiscal-waterfall-dist, household-npv, policy-scenario
- Total: 9

### Step 6: Verify component files exist

Check that all 9 component files exist:
```bash
ls -la client/src/components/charts/*Widget.tsx
```

## Self-check

1. `chartRegistry.ts` contains exactly 9 entries
2. All 4 new entries use the correct lazy import pattern matching existing entries
3. Named exports in the `.then()` callback match the actual named exports in each component file
4. `npx tsc --noEmit` passes with zero errors
5. All 9 component files exist on disk
6. No Phase 1 widgets were removed or modified

## Handoff

P11.1 will prepare all data files for deployment. P11.2 will insert the 4 new data-demo tags into the report content. P11.4 will run a full build verification including all 9 widgets.
