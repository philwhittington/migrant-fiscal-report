# Task P12.1: Deploy Phase 2

## Objective

Perform a clean production build of the migrant fiscal report site with all Phase 2 synthetic population content and widgets, commit to git, push to remote, and deploy to Cloudflare Pages. Update project tracking files to reflect Phase 2 completion.

## Inputs

- All source files from Phases 9-11 (content, widgets, data, registry)
- `.agent/qa-data-report-phase2.md` — data QA report (must show all PASS)
- `.agent/qa-widget-report-phase2.md` — widget QA report (must show all PASS)
- `.agent/SCRATCHPAD.md` — project status tracker
- `.agent/LEARNINGS.md` — project learnings log

## Expected outputs

- Clean production build in `dist/`
- Git commit with all Phase 2 changes
- Deployed site on Cloudflare Pages
- Updated `.agent/SCRATCHPAD.md`
- Updated `.agent/LEARNINGS.md`

## Execution instructions

### Step 1: Pre-flight checks

Before building, verify that QA reports show no failures:

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
grep -c "FAIL" .agent/qa-data-report-phase2.md
grep -c "FAIL" .agent/qa-widget-report-phase2.md
```

If either report contains FAIL entries, stop and resolve them before proceeding. Do not deploy with known data or build failures.

### Step 2: Clean build

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
rm -rf dist
npm run build
```

Verify the build succeeds with zero errors. Note the output directory structure — Cloudflare Pages expects the static files to be in a specific location (likely `dist/public` or `dist/client`).

Check build output:
```bash
ls -la dist/
du -sh dist/
```

### Step 3: Verify built assets

Confirm the built output includes:
- HTML entry point
- JavaScript bundles (including lazy-loaded widget chunks)
- Data JSON files in the public data directory
- CSS files

```bash
find dist/ -name "*.json" | head -20
find dist/ -name "*.js" | wc -l
```

### Step 4: Git commit

Stage all Phase 2 changes:

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
git add content/migrant-fiscal-impact.md
git add client/src/components/charts/NPVDistributionWidget.tsx
git add client/src/components/charts/FiscalWaterfallDistWidget.tsx
git add client/src/components/charts/HouseholdNPVWidget.tsx
git add client/src/components/charts/PolicyScenarioWidget.tsx
git add client/src/components/charts/chartRegistry.ts
git add client/public/data/synth-*.json
git add data/output/synth-*.json
git add .agent/qa-data-report-phase2.md
git add .agent/qa-widget-report-phase2.md
git add .agent/SCRATCHPAD.md
git add .agent/LEARNINGS.md
```

Also add any other modified files that are part of Phase 2:
```bash
git status
# Review and add any remaining Phase 2 files
```

Commit:
```bash
git commit -m "Phase 2: Synthetic population layer with distributional widgets

Adds synthetic population methodology (Section 2.6), distributional findings
across Sections 3-7, and four new interactive widgets: NPV distribution
histogram, fiscal waterfall with uncertainty bands, household NPV explorer,
and policy scenario simulator. Total widgets: 9. All data QA and build QA
passed."
```

### Step 5: Push to remote

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
git push
```

If the push fails due to upstream changes:
```bash
git pull --rebase
git push
```

### Step 6: Deploy to Cloudflare Pages

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
npx wrangler pages deploy dist/public --project-name migrant-fiscal-report --branch main
```

Note: adjust the path (`dist/public`) to match the actual build output directory identified in Step 3. If the build outputs to `dist/client` or just `dist/`, adjust accordingly.

Wait for the deployment to complete. Note the deployment URL.

### Step 7: Verify deployment

After deployment, check the live URL:
- Confirm the site loads
- Confirm at least one widget loads (check browser console for errors if possible)
- Note the deployment URL for the handoff

### Step 8: Update SCRATCHPAD.md

Read `.agent/SCRATCHPAD.md` and update the Phase 2 status:
- Change Phase 2 status to "DEPLOYED"
- Add deployment date and URL
- Note any caveats or follow-up items

### Step 9: Update LEARNINGS.md

Read `.agent/LEARNINGS.md` and add Phase 2 learnings. Include:
- Key technical decisions (zero-inflated log-normal fitting, browser-side reweighting for policy scenarios)
- What worked well (e.g., SVG-only charts, synthetic population validation approach)
- What was difficult or surprising
- Recommendations for future phases

## Self-check

1. `npm run build` succeeded with zero errors
2. Git commit includes all Phase 2 files
3. Git push succeeded
4. Cloudflare Pages deployment succeeded
5. Live site loads without errors
6. SCRATCHPAD.md reflects "DEPLOYED" status
7. LEARNINGS.md includes Phase 2 findings
8. No QA failures were deployed

## Handoff

Phase 2 is complete. The deployed site now includes:
- Phase 1: Group-mean fiscal analysis with 5 interactive widgets
- Phase 2: Synthetic population distributional analysis with 4 additional widgets (9 total)

Future phases may include: peer review feedback incorporation, additional nationality breakdowns, time-series updates with new census data, or PDF export of the report with embedded chart images.
