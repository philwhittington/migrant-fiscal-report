# Task P11.4: Widget build QA for Phase 2

## Objective

Run full build verification for the project including all 9 widgets. Verify TypeScript compilation, production build, widget file presence, registry completeness, and data file availability. Write a QA report documenting all checks.

## Inputs

- `client/src/components/charts/chartRegistry.ts` — the registry with 9 entries
- `client/src/components/charts/*Widget.tsx` — all 9 widget component files
- `client/public/data/*.json` — all data files (Phase 1 and Phase 2)
- `package.json` — project dependencies and build scripts

## Expected outputs

- `.agent/qa-widget-report-phase2.md` — QA report documenting all checks
- All build errors fixed (if any)

## Execution instructions

### Step 1: Install dependencies

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
npm install
```

Verify no install errors. If there are peer dependency warnings, note them but proceed unless they are blocking errors.

### Step 2: TypeScript compilation check

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
npx tsc --noEmit
```

If there are TypeScript errors:
1. Read each error message carefully
2. Fix the error in the relevant file
3. Re-run `npx tsc --noEmit` until clean

Common Phase 2 widget errors to watch for:
- Missing type imports
- Incorrect useQuery generic types
- SVG attribute type mismatches (e.g., `string` vs `number` for SVG coordinates)
- Named export mismatches in chartRegistry.ts

### Step 3: Production build

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
npm run build
```

If the build fails:
1. Read the error output
2. Fix the issue
3. Re-run `npm run build` until successful

Note the build output size and any warnings about large chunks (the Phase 2 widgets may increase bundle size).

### Step 4: Verify all 9 widget files exist

```bash
ls -la client/src/components/charts/*Widget.tsx
```

Confirm 9 files:
1. NPVCalculatorWidget.tsx
2. FiscalWaterfallWidget.tsx
3. RetentionExplorerWidget.tsx
4. RV2021ShiftWidget.tsx
5. NationalityConvergenceWidget.tsx
6. NPVDistributionWidget.tsx
7. FiscalWaterfallDistWidget.tsx
8. HouseholdNPVWidget.tsx
9. PolicyScenarioWidget.tsx

Note: actual file names may vary slightly from this list — match against what exists.

### Step 5: Verify registry completeness

Read `client/src/components/charts/chartRegistry.ts` and confirm:
- 9 entries present
- Each entry has a component (lazy import) and a title
- Each lazy import points to an existing file
- Each named export in the `.then()` callback matches the actual export

### Step 6: Verify all data files

```bash
ls -lh client/public/data/*.json
```

Confirm all required data files are present. Minimum set:

Phase 1:
- npv-by-visa-age.json
- fiscal-components-by-migrant-type.json
- (other Phase 1 files)

Phase 2:
- synth-npv-distributions.json
- synth-fiscal-distributions.json
- synth-household-npv.json
- synth-population-summary.json

### Step 7: Verify data files are valid JSON

```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
for f in client/public/data/*.json; do
  python3 -c "import json; json.load(open('$f')); print(f'OK: $f')" 2>&1
done
```

### Step 8: Check for console errors (optional)

If the dev server can be started:
```bash
cd /Users/PhilWhittington/Projects/migrant-fiscal-report
npm run dev &
sleep 5
# Use curl to check that data endpoints respond
curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/data/synth-npv-distributions.json
kill %1
```

### Step 9: Write QA report

Create `.agent/qa-widget-report-phase2.md`:

```markdown
# Phase 2 widget build QA report

## Summary
- TypeScript compilation: PASS/FAIL
- Production build: PASS/FAIL
- Widget files present: X/9
- Registry entries: X/9
- Data files present: X/X
- Data files valid JSON: X/X

## TypeScript compilation
- Status: PASS/FAIL
- Errors fixed: [list any errors fixed]

## Production build
- Status: PASS/FAIL
- Build size: X MB
- Warnings: [list any warnings]
- Errors fixed: [list any errors fixed]

## Widget inventory
| # | Demo ID | Component file | Named export | Registry entry | Status |
|---|---------|---------------|--------------|----------------|--------|
| 1 | npv-calculator | NPVCalculatorWidget.tsx | NPVCalculatorWidget | Yes | PASS |
| 2 | fiscal-waterfall | FiscalWaterfallWidget.tsx | FiscalWaterfallWidget | Yes | PASS |
| ... | | | | | |

## Data file inventory
| # | File | Size | Valid JSON | Status |
|---|------|------|-----------|--------|
| 1 | npv-by-visa-age.json | 45 KB | Yes | PASS |
| ... | | | | |

## Issues found and resolved
1. [describe any issues fixed during QA]

## Issues remaining
1. [describe any unresolved issues, if any]
```

## Self-check

1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. All 9 widget files exist
4. All 9 registry entries are present and correct
5. All data files are present and valid JSON
6. QA report exists at `.agent/qa-widget-report-phase2.md`
7. No unresolved blocking issues

## Handoff

P12.1 will perform the final clean build and deploy. All widget and data issues must be resolved before deployment.
