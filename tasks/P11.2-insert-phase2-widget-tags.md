# Task P11.2: Insert Phase 2 widget tags into report content

## Objective

Insert 4 new `data-demo` widget tags into the report markdown at the correct locations, bringing the total from 5 (Phase 1) to 9 widget embeds. Each tag must be placed where it best supports the surrounding prose.

## Inputs

- `content/migrant-fiscal-impact.md` — the report content, already updated with Phase 2 distributional text (P9.1, P9.2, P9.3)
- Knowledge of the 4 new demo IDs: `npv-distribution`, `fiscal-waterfall-dist`, `household-npv`, `policy-scenario`

## Expected outputs

- `content/migrant-fiscal-impact.md` — updated with 4 new data-demo tags (9 total)

## Execution instructions

### Step 1: Read the full report

Read `content/migrant-fiscal-impact.md` in its entirety. Map the section structure:
- Section 1: Introduction/Executive summary
- Section 2: Methodology
- Section 3: Fiscal components analysis
- Section 4: Visa type analysis
- Section 5: Nationality analysis
- Section 6: Retention analysis
- Section 7: NPV analysis
- Section 8: (varies)
- Section 9: Conclusion

Locate all 5 existing `data-demo` tags and note their positions.

### Step 2: Identify insertion points

For each new widget, find the optimal insertion point:

1. **`fiscal-waterfall-dist`** — Insert in **Section 3**, immediately after the existing `data-demo="fiscal-waterfall"` tag. The distributional waterfall should follow the point-estimate waterfall, providing a natural "and here's the uncertainty" progression.

2. **`npv-distribution`** — Insert in **Section 7**, after the existing `data-demo="npv-calculator"` tag. The distribution histogram complements the lifecycle trajectory chart — trajectory shows the time path, distribution shows the spread of outcomes.

3. **`household-npv`** — Insert in **Section 7**, after the `npv-distribution` tag. Create a new subsection heading "### Household-level analysis" before the tag. This subsection explores family-unit NPV, which is a natural extension of the individual NPV discussion.

4. **`policy-scenario`** — Insert in **Section 9** (Conclusion). Create a new subsection heading "### What if settings changed?" before the tag. This positions the scenario explorer as the forward-looking conclusion to the analysis.

### Step 3: Insert each tag

For each insertion, use this exact format — the tag must be on its own line with blank lines before and after:

```markdown

<div data-demo="demo-id-here"></div>

```

That is: one blank line, the div tag, one blank line.

### Step 4: Insert subsection headings where needed

For `household-npv` and `policy-scenario`, add subsection headings:

Before the `household-npv` tag:
```markdown
### Household-level analysis

Understanding the fiscal impact of complete family units — including spouses and children — provides a more realistic picture than individual analysis alone.

<div data-demo="household-npv"></div>

```

Before the `policy-scenario` tag:
```markdown
### What if settings changed?

The interactive scenario explorer below allows readers to examine how changes to visa composition, retention rates, and NZ Super eligibility thresholds affect the aggregate fiscal picture.

<div data-demo="policy-scenario"></div>

```

### Step 5: Count and verify all tags

After all insertions, search the file for `data-demo=` and count occurrences. There should be exactly 9:

Phase 1 (5 existing):
1. `npv-calculator`
2. `fiscal-waterfall`
3. `retention-explorer`
4. `rv2021-shift`
5. `nationality-convergence`

Phase 2 (4 new):
6. `fiscal-waterfall-dist`
7. `npv-distribution`
8. `household-npv`
9. `policy-scenario`

### Step 6: Verify each tag is on its own line

Read the lines around each tag to confirm:
- The tag is on its own line (not inline with prose)
- There is a blank line before the tag
- There is a blank line after the tag
- The demo ID matches one of the 9 registered IDs in chartRegistry.ts

## Self-check

1. Exactly 9 `data-demo` tags in the file
2. Each new tag is in the correct section (fiscal-waterfall-dist in S3, npv-distribution in S7, household-npv in S7, policy-scenario in S9)
3. Each tag is on its own line with blank lines before and after
4. New subsection headings use sentence case
5. Introductory sentences before household-npv and policy-scenario are present
6. No existing tags were moved or removed
7. All 9 demo IDs match the IDs registered in chartRegistry.ts

## Handoff

P11.3 will verify all data claims match source files. P11.4 will run a full build and verify all 9 widgets render.
