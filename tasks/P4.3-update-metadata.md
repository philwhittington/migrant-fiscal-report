# Task P4.3: Update page metadata and strip Pacific LLM content

## Objective

Remove all traces of the Pacific LLM / VAT report from the project and update metadata to reflect the migrant fiscal impact report. This includes page titles, meta tags, hardcoded text in components, and orphaned data files.

## Inputs

- `client/src/pages/Reader.tsx` — the main reader page component
- `client/index.html` — the HTML entry point with meta tags
- `client/src/components/charts/VATDemoWidget.tsx` — Pacific LLM widget (to be removed or kept as reference)
- `client/src/components/charts/ImplementationPlanWidget.tsx` — Pacific LLM widget
- `client/src/components/charts/ComplianceReviewWidget.tsx` — Pacific LLM widget
- `client/src/components/charts/chartRegistry.ts` — has old Pacific LLM entries
- `data/vat-register.json`, `data/source-a-grt-register.json`, `data/source-b-business-licences.json`, `data/source-c-vat-interest-log.json`, `data/implementation-plan.json` — old Pacific LLM data files

## Expected outputs

- `client/src/pages/Reader.tsx` — updated with new title and description
- `client/index.html` — updated meta tags
- `client/src/components/charts/chartRegistry.ts` — old Pacific LLM entries removed (keep the 5 new migrant fiscal entries from P3.6)
- Old Pacific LLM data files deleted from `data/`
- Old Pacific LLM widget files can be kept (they will tree-shake out since nothing imports them) or deleted -- your choice, but do NOT delete them if any remaining code imports them

## Execution instructions

### Step 1: Update Reader.tsx

Read `client/src/pages/Reader.tsx`. Look for:
- Any hardcoded title like "Pacific LLM" or "VAT" or "Marshall Islands"
- Description text
- Header text
- Any references to old data files

Replace with:
- Title: "The fiscal impact of migration to New Zealand"
- Description: "An interactive analysis of migrant fiscal contributions by visa type, arrival age, and nationality"
- Author: "Heuser|Whittington" (if there is an author field)

Be careful to only change text content, not component structure. Read the entire file first to understand the layout.

### Step 2: Update index.html

Read `client/index.html`. Update:
- `<title>` tag: "The fiscal impact of migration to New Zealand"
- `<meta name="description">`: "An interactive analysis of migrant fiscal contributions to New Zealand, examining lifetime NPV by visa category, income convergence, retention rates, and the impact of the 2021 Residence Visa."
- Any `<meta property="og:...">` tags: update title, description, site_name
- Remove any Pacific LLM specific meta content

### Step 3: Clean up chartRegistry.ts

Read `client/src/components/charts/chartRegistry.ts`. It should now contain:
- 3 old entries: vat-registrants, implementation-plan, compliance-review
- 5 new entries: npv-calculator, nationality-convergence, retention-explorer, rv2021-shift, fiscal-waterfall

Remove the 3 old entries. Keep the 5 new entries. The final file should have exactly 5 entries.

### Step 4: Delete old Pacific LLM data files

Delete these files from the `data/` root directory (NOT from data/output/ or data/processed/):
- `data/vat-register.json`
- `data/source-a-grt-register.json`
- `data/source-b-business-licences.json`
- `data/source-c-vat-interest-log.json`
- `data/implementation-plan.json`

Before deleting, verify each file exists and is indeed a Pacific LLM artifact (not something reused by the migrant report).

### Step 5: Check for other Pacific LLM references

Search the entire `client/src/` directory for strings like "Pacific", "LLM", "VAT", "Marshall Islands", "DCTRT", "GRT", "vat-register":

```bash
grep -r "Pacific\|LLM\|Marshall Islands\|DCTRT\|vat-register\|GRT" client/src/ --include="*.tsx" --include="*.ts" -l
```

For each file found:
- Read it to understand the context
- If it contains hardcoded Pacific LLM text in UI-visible locations, update it
- If it is an old Pacific LLM widget file (VATDemoWidget, ImplementationPlanWidget, ComplianceReviewWidget), leave the file but ensure nothing imports it
- If chartRegistry.ts no longer references it, the code will tree-shake out

### Step 6: Check for floating header

Some report readers have a floating header that shows the report title as you scroll. Search for this in components:

```bash
grep -r "header\|title\|floating" client/src/ --include="*.tsx" --include="*.ts" -l
```

If a floating header component exists with hardcoded text, update it to either:
- Read the title from report.json dynamically
- Use "The fiscal impact of migration to New Zealand"

## Self-check

1. `client/index.html` title is "The fiscal impact of migration to New Zealand".
2. `Reader.tsx` contains no Pacific LLM text.
3. `chartRegistry.ts` has exactly 5 entries (all migrant fiscal widgets).
4. Old data files are deleted from `data/` root.
5. `grep -r "Pacific" client/src/ --include="*.tsx" --include="*.ts"` returns no hits in UI-visible text (comments are OK).
6. The project still compiles: `cd /Users/PhilWhittington/Projects/migrant-fiscal-report && npx tsc --noEmit` (warnings about unused old widget files are acceptable).

## Handoff

After this task, the project should be fully rebranded for the migrant fiscal report. Phase 5 QA tasks will verify the full build and content.
